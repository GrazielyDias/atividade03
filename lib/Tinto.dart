import 'package:flutter/material.dart';
import 'main.dart';

class Tinto extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Página de Destino"),
      ),
      body: Center(
        child: Text("Bem-vindo à página de destino!"),
      ),
    );
  }
}
